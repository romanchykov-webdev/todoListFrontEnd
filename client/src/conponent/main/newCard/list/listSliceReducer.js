import {createSlice} from "@reduxjs/toolkit";


const listSliceReducer=createSlice({
    name:'list',
    initialState:{

    },
    reducers:{

    }
})

export default listSliceReducer.reducer
export const {}=listSliceReducer.actions