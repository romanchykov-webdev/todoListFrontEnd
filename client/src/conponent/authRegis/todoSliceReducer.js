import {createSlice} from "@reduxjs/toolkit";


const todoSliceReducer=createSlice({
    name:"todoSliceReducer",
    initialState:{

    },reducers:{

    }
})

export default todoSliceReducer.reducer
export const {}=todoSliceReducer.actions